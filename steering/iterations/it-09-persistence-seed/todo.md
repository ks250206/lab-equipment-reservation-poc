# TODO — it-09-persistence-seed

- [x] `ENVIRONMENT` / `PERSISTENCE_PROFILE` と Compose 分割（dev-file vs Keycloak→Postgres）
- [x] Postgres init で `keycloak` DB
- [x] `app.seeding` 開発シード（装置 33 / ユーザー 8）と `just seed-dev`
- [x] `Settings` の `.env` 探索（`backend/.env` とルート `../.env`）
- [x] README / `doc/` / `AGENTS.md` / steering 更新
- [x] `just backend-test` を `uv run --extra test` に修正
