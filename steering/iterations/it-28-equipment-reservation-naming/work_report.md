# 作業レポート — it-28-equipment-reservation-naming

- `device-reservation` / `device_reservation` / `device-images` を `equipment-reservation` / `equipment_reservation` / `equipment-images` に置換。
- 既存開発者向けに `doc/local-development.md` 冒頭へ移行メモを追記。
