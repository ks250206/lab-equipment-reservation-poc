# Implementation (Iteration 7 — Refactor & Polish)

_Not started as of the last repository pass._ Intended scope:

- Raise and stabilise automated test coverage (backend + frontend) toward project goals.
- Resolve remaining static-analysis noise (`ty`, optional stricter rules) without changing product behaviour.
- Trim oversized modules (>500 lines) where it improves clarity.
- Polish operator docs (`README`, OpenAPI exposure if adopted) after code stabilises.
