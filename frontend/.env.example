# Keycloak（ブラウザから直接接続。README のクライアント設定と一致させる）
VITE_KEYCLOAK_URL=http://localhost:8080
VITE_KEYCLOAK_REALM=master
VITE_KEYCLOAK_CLIENT_ID=equipment-reservation

# API（既定は相対パス。Vite の proxy が /api を FastAPI に転送）
VITE_API_BASE=/api
